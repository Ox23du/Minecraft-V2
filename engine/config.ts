// Default field of view for the player's perspective camera in degrees.
export const DEFAULT_FOV = 110

// Default camera near and far clipping planes.
export const DEFAULT_NEAR = 0.1
export const DEFAULT_FAR = 1000

// Default fog start and end distances.
export const DEFAULT_FOG_START = 20
export const DEFAULT_FOG_END = 60
